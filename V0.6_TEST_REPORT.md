# V0.6 Test Report

## 核心回归

```text
90 passed
```

执行范围：

- `tests/test_platform_core_v01.py`
- `tests/test_gpu_simulator_v02.py`
- `tests/test_mock_stage_v02.py`
- `tests/test_platform_mcp_v03.py`
- `tests/test_platform_agent_v04.py`
- `tests/test_platform_rag_v05.py`
- `tests/test_task_planning_v06.py`

## V0.6 专项

```text
15 passed
```

覆盖：

- Natural Language -> TaskSpec
- 平台 defaults
- task_type -> priority
- full pipeline
- single-stage pipeline
- parallel pipeline
- GPU shared/exclusive
- 显存 GB -> MB
- 多 Dataset
- missing field
- invalid Stage
- local YAML atomic write
- Agent task_planning intent
- submit request pre-model block
- deploy/package settings
- deterministic planning eval

## Task Planning Eval

```text
cases=8
passed=8
accuracy=1.000
```

文件：

```text
eval/task_planning_cases.json
```

## 原平台脚本回归

以下 standalone 回归继续通过：

- `python3 tests/test_task_priority_queue.py`
- `python3 tests/test_task_submit_scheduler.py`
- `python3 tests/test_platform_restart_cleanup.py`

## Runtime 部署检查

使用临时 Runtime 执行 `scripts/deploy_ci_cloud.sh`：

- `platform_planning` 成功同步
- `task_planning_defaults.yaml` 成功同步
- 部署后的 `scripts/dataops_agent.py plan-task` 成功生成 YAML
- 生成 YAML 可被 YAML loader 读取
- release priority / concurrency / GPU exclusive policy / dataset path 与期望一致

## 环境限制

开发沙箱仍未安装真实 Airflow Python package，因此完整 `pytest -q` 中直接 import Airflow DAG 的历史测试无法 collection。

V0.6 Task Planning 本身不依赖 Airflow package、真实 GPU 或外部 LLM API，可在当前沙箱完整运行。
