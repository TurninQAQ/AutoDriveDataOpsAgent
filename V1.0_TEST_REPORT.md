# V1.0 Test Report

## 新增专项

```text
tests/test_hardening_v10.py
```

V1.0 专项：

```text
7 passed
```

覆盖：

1. Trace retention + max files。
2. Audit size rotation + bounded backups。
3. doctor dependency-light + simulated GPU readiness。
4. Local E2E：Agent/HITL/Precondition/Verification/GPU diagnosis/soft preemption/recovery/Trace/Audit。
5. Approval concurrent claim exactly-one winner。
6. deploy/platform retention/hardening 配置同步。

## 累计 dependency-light pytest

命令：

```bash
PYTHONPATH=. pytest -q tests \
  --ignore=tests/test_dag_preemption_queue.py \
  --ignore=tests/test_exclusive_gpu_stages.py
```

结果：

```text
138 passed
```

## Standalone regression

以下继续返回 0：

```bash
python tests/test_task_priority_queue.py
python tests/test_task_submit_scheduler.py
python tests/test_platform_restart_cleanup.py
```

## 完整 pytest

当前环境 `pytest -q` 仍在 collection 阶段出现 3 个历史 Airflow 依赖错误：

```text
dags/batch_pipeline_universal_test.py
dags/dataset_schedulers_test.py
tests/test_dag_preemption_queue.py
```

统一原因：

```text
ModuleNotFoundError: No module named 'airflow'
```

V1.0 没有新增 collection/test failure。

## Runtime deploy smoke

临时 Runtime deploy 后，直接使用部署后的 `opt_airflow` 执行：

```text
dataops-agent doctor --json     -> ready_dependency_light=true
dataops-agent e2e --json        -> ok=true
dataops-agent observability-maintenance --json -> success
```

同时确认：

```text
$RUNTIME_DIR/opt_airflow/platform_hardening/e2e.py
```

真实存在。
