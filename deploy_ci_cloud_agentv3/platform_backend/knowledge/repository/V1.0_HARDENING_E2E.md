# V1.0 Hardening / End-to-End Validation

## 1. 目标

V1.0 不扩大 Agent 的业务写权限，不新增 Multi-Agent、Web UI 或平台状态机。
本版本用于对 V0.1～V0.9 已有能力做工程收口，重点解决：

1. 无真实 GPU / 无 Airflow Python 包 / 无 MCP/LangGraph 依赖时，仍可本地执行确定性的完整控制链回归。
2. 能明确区分 dependency-light 已验证能力和 full runtime 外部依赖准备状态。
3. Trace/Audit 不再无限增长。
4. HITL approval 在并发 claim 场景下继续满足单执行者语义。
5. deploy 后的 Runtime 必须能够运行同一套 doctor/e2e/maintenance 入口。

## 2. 新增 platform_hardening

新增：

```text
platform_hardening/
├── __init__.py
├── doctor.py
└── e2e.py
```

该 package 只用于开发验证和运行环境硬化，不进入 Platform Core 业务状态机。

## 3. dataops-agent doctor

新增：

```bash
dataops-agent doctor
dataops-agent doctor --strict
dataops-agent doctor --json
```

普通模式判断 dependency-light 开发环境是否可用，检查：

- Python
- state/task config/session/approval/trace/audit 目录可写
- Mock Stage
- Task Planning defaults
- Knowledge source
- GPU Runtime（模拟模式可直接验证设备）

同时报告 full runtime 外部依赖：

- Airflow Python package
- MCP Python SDK
- LangGraph
- langchain-openai
- Docker
- nvidia-smi 或模拟 GPU Runtime

`--strict` 只有 full runtime 条件全部满足时才返回 0。

## 4. Dependency-light E2E

新增：

```bash
dataops-agent e2e
dataops-agent e2e --root /tmp/dataops-e2e
dataops-agent e2e --json
```

E2E 使用真实：

- Heuristic Agent planner / synthesizer
- Sequential Agent Workflow
- Natural Language Task Planning
- 原平台 config validator
- Agent Policy
- HITL Approval Store
- Precondition
- ActionVerifier
- Trace/Audit
- SimulatedGPURuntime
- `scripts/mock_stage.py`
- `scripts/validate_json.py`

只把当前开发环境中不存在的 Airflow/Docker 外部执行面替换为 `LocalScenarioToolClient`。
该 Tool Client 实现和 MCP 相同的 Tool Contract，但不宣称等价于真实 Airflow runtime。

固定 E2E 场景：

```text
Mock Segment -> Validator
        ↓
自然语言 submit reprocess
        ↓
TaskSpec / validation
        ↓
Pending Approval
        ↓
Approve -> mutation -> verification
        ↓
Task status query
        ↓
Simulated GPU memory pressure
        ↓
Agent GPU diagnosis
        ↓
submit high-priority release
        ↓
low task active -> draining
        ↓
Stage boundary checkpoint
        ↓
release active
        ↓
release finish
        ↓
low task Recovery run -> active
        ↓
priority change -> HITL -> verify
        ↓
Trace/Audit persisted
```

注意：E2E backend 的 task priority 也调用原 `normalize_task_priority_config()`，避免直接从 YAML 缺失字段猜 priority。

## 5. Observability retention / rotation

新增环境变量：

```text
PLATFORM_AGENT_TRACE_RETENTION_DAYS=14
PLATFORM_AGENT_TRACE_MAX_FILES=5000
PLATFORM_AGENT_AUDIT_MAX_BYTES=20971520
PLATFORM_AGENT_AUDIT_BACKUP_COUNT=5
```

0 表示关闭对应限制。

TraceStore 新增：

```text
prune_traces()
rotate_audit()
maintenance()
```

Agent Runtime 创建 TraceRecorder 时会执行一次 maintenance；每次 Audit 完成后也会再次执行有界 maintenance，覆盖长时间 chat 进程。
也可以手动运行：

```bash
dataops-agent observability-maintenance
```

Trace 同时受：

- retention days
- max file count

限制。

Audit 使用 size-based rotation：

```text
audit.jsonl
audit.jsonl.1
audit.jsonl.2
...
```

## 6. 并发 HITL 硬化

V0.7 已经实现每个 approval 的 `fcntl` 锁和：

```text
pending -> executing -> executed/failed/verification_failed
```

V1.0 新增并发回归测试：两个并发执行者同时 claim 同一个 approval，只允许一个成功进入 `executing`，另一个必须因为状态不再是 pending 而失败。

## 7. Deploy 修改

`platform_hardening/` 现在随 deploy 同步到：

```text
$RUNTIME_DIR/opt_airflow/platform_hardening
```

支持：

```text
AIRFLOW_PLATFORM_HARDENING_DIR
```

`platform.env` 新增 Trace/Audit retention/rotation 配置。

发布前已从临时部署后的 Runtime 直接运行：

```text
dataops-agent doctor --json
dataops-agent e2e --json
dataops-agent observability-maintenance --json
```

均正常。

## 8. V1.0 不做

1. 不新增 Agent Write Tool。
2. 不开放 restart_platform。
3. 不修改 Queue / Soft Preemption / Recovery / GPU Reservation 生产状态机。
4. 不做 Multi-Agent。
5. 不做 Web UI / Dashboard。
6. 不假装当前沙箱执行过真实 Airflow DAG。
7. 不假装当前沙箱执行过真实 MCP v2 Client/Server 或 LangGraph runtime。
8. 不要求真实 GPU。

## 9. Full Runtime 尚需在具备依赖的环境补充

当前沙箱没有：

- Airflow Python package
- `mcp==2.0.0`
- LangGraph
- langchain-openai
- 真实 GPU

因此 V1.0 的本地成功标准是 dependency-light E2E 全通过。
若未来在完整环境运行，先执行：

```bash
dataops-agent doctor --strict
```

然后再补真实 Airflow/MCP/LangGraph smoke；只有真实测试暴露问题时才新增后续版本功能。
