Repository test hygiene summary
===============================

Collection: PASS — 458 tests collected.
Airflow fixture: repo-local fallback was added for the test-only DAG fixture; collection succeeds without /opt/airflow.
Repository-wide execution: TIMEOUT after 180 seconds in a historical GPU/runtime area; not treated as a V1.8 product pass.
Known environment failures observed separately:
- GPU simulator runtime-default test expects Nvidia runtime but this environment selects simulated runtime.
- Deploy executable test expects a `python` executable that is absent in the environment.
- Exclusive GPU runtime test is a long-running environment/stress path.

No V1.8 change touched GPU allocator business semantics, mutation core semantics, RAG, or MCP contracts.
