# V0.4 Test Report

## 核心自动化回归

执行：

```bash
python3 -m pytest -q \
  tests/test_platform_core_v01.py \
  tests/test_gpu_simulator_v02.py \
  tests/test_mock_stage_v02.py \
  tests/test_platform_mcp_v03.py \
  tests/test_platform_agent_v04.py
```

最终发布前结果：

```text
62 passed
```

第一次完整核心回归中，V0.2 的 `validate_fail` Validator 子进程曾出现一次 5 秒 timeout；该单测立即单独重跑通过，随后完整 62 项再次执行全部通过，未复现。没有为此修改 V0.2 业务逻辑。

## V0.4 Agent 专项覆盖

- V0.3 8 个只读 Tool Contract 固定
- Task status Tool Selection
- GPU diagnosis Tool Selection
- Stage failure + OOM diagnosis
- 平台 Health
- Task List
- 写请求 model 前拦截
- 模型伪造 write Tool 拦截
- Thread history 持久化
- Tool error 暴露
- MCP SDK 缺失错误提示
- MCP structured_content 读取
- 日志 Prompt Injection 不产生二次 Tool 行为
- LangGraph graph wiring
- LangGraph thread_id
- LangGraph 依赖缺失错误提示
- CLI help 不依赖可选运行时包
- deploy 后 platform_agent package 不被入口脚本遮蔽

## 环境限制

当前开发沙箱：

- 无 Airflow Python package
- 无 mcp Python SDK
- 无 LangGraph
- 无 langchain-openai
- 无可用外部模型 API Key
- PyPI DNS 不可访问

因此：

1. V0.4 的 Agent Node / Policy / Tool Selection / State / Synthesis 使用确定性 Fake/Heuristic Provider 完成回归。
2. MCP Client 的 v2 `Client -> list_tools/call_tool -> structured_content` 通过接口 Fake 做适配测试。
3. LangGraph StateGraph 通过 API-compatible Fake Graph 验证 wiring。
4. 真实依赖安装后仍需补跑一条协议/框架集成 smoke test。

这不影响 V0.4 的核心业务逻辑验证，但不能把“Fake API contract test”描述成“真实 LangGraph/MCP 网络运行已验证”。
