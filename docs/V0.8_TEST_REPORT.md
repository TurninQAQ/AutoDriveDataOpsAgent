# V0.8 Test Report

## V0.8 专项

```text
tests/test_action_verification_v08.py
12 passed
```

覆盖：

1. Verification Tool 不暴露给模型。
2. priority config/queue 验证。
3. submit Task/DAG/Queue/DagRun 验证。
4. stop DagRun/container/reservation/queue 验证。
5. resume baseline/new DagRun 验证。
6. delete artifact/runtime/Airflow metadata 验证。
7. Airflow evidence 不可用时不能误报成功。
8. eventual-consistency Observe retry。
9. 只有验证通过才 `executed`。
10. Write Tool ok 但验证失败 -> `verification_failed`。
11. Task YAML 删除后 Verification Snapshot 仍可工作。

## 核心回归

V0.1 ~ V0.8 dependency-light 核心测试：

```text
122 passed
```

原 standalone regression：

```text
python tests/test_task_priority_queue.py
python tests/test_task_submit_scheduler.py
python tests/test_platform_restart_cleanup.py
```

均通过。

## 当前环境限制

当前沙箱没有 Airflow Python package，因此完整 `PYTHONPATH=. pytest -q` 仍有原有 3 个 collection error：

- `dags/batch_pipeline_universal_test.py`
- `dags/dataset_schedulers_test.py`
- `tests/test_dag_preemption_queue.py`

均为 `ModuleNotFoundError: No module named 'airflow'`，V0.8 没有新增 collection error。
