# V0.5 Test Report

## Core pytest

V0.1 ~ V0.5 当前可在开发沙箱执行的核心测试：

```text
74 passed
```

包含 V0.5 新增 12 个专项用例。

## V0.5 Retrieval Evaluation

`eval/rag_cases.json`：8 个固定知识检索案例。

结果：

```text
Hit@K = 1.000
MRR   = 0.854
```

覆盖：

- GPU wait
- draining / soft preemption
- Recovery checkpoint
- Docker dataset token matching
- Validate failure
- Airflow PostgreSQL metadata background
- GPU wait timeout semantics
- RAG realtime grounding boundary

## V0.5 Agent/RAG 专项覆盖

1. Knowledge index 持久化。
2. Source fingerprint 变化后自动 refresh。
3. 并发 `ensure()` 使用文件锁和 atomic publish，不产生损坏 index。
4. GPU / draining / Docker domain retrieval。
5. 静态平台问题可 RAG-only 回答，不调用 MCP。
6. Diagnosis 同时保留 live Tool Evidence 和 Runbook source。
7. Runbook 不能覆盖与其冲突的实时 GPU Evidence。
8. Retrieval error 不阻断实时 MCP diagnosis。
9. Retrieved document prompt injection 不能触发 write tool 或第二轮 tool loop。
10. CLI 暴露 knowledge build/status/search/eval。
11. AgentSettings 持久化 RAG 配置。
12. deploy 脚本同步 platform_rag 和 knowledge。

## Deploy smoke test

实际将 `scripts/deploy_ci_cloud.sh` 部署到临时 Runtime，确认：

- `platform_rag/` 被复制。
- `knowledge/` 被复制。
- repository docs/config 被同步。
- Runtime PYTHONPATH 可以 import `platform_rag`。
- Runtime knowledge 可以构建索引并完成检索。

结果：PASS。

## 原平台脚本回归

以下继续通过：

- `tests/test_task_priority_queue.py`
- `tests/test_task_submit_scheduler.py`
- `tests/test_platform_restart_cleanup.py`

## 当前环境限制

开发沙箱仍未安装 Airflow / MCP SDK / LangGraph / langchain-openai，因此完整 `pytest -q` 中直接 import Airflow DAG 的历史测试仍不能在该沙箱 collection。

V0.5 RAG 本身不依赖这些可选包，因此 Index、Retrieval、Evaluation、RAG-only Agent 路径已真实运行。
