# V0.2 本地验证结果

## 自动化测试

执行：

```bash
python -m pytest -q \
  tests/test_gpu_simulator_v02.py \
  tests/test_mock_stage_v02.py \
  tests/test_platform_core_v01.py
```

结果：

```text
35 passed
```

覆盖 GPU Simulator、GPUAllocator、Mock Stage、原 Validator、V0.1 Platform Core 和部署复制兼容。

## 原平台脚本回归

以下继续通过：

```bash
python tests/test_task_priority_queue.py
python tests/test_task_submit_scheduler.py
python tests/test_platform_restart_cleanup.py
```

其中 `test_task_priority_queue.py` 覆盖高优任务触发 draining、队列切换以及 preempted task Recovery 相关逻辑。

## GPU Simulator CLI

已验证：

```text
init
set-memory
show
```

状态文件修改和重新读取一致。

## 当前环境限制

当前开发沙箱没有安装 Airflow Python 包，因此旧的 `test_exclusive_gpu_stages.py` / `test_dag_preemption_queue.py` 中直接 import Airflow DAG Runtime 的部分不能在此环境执行。

V0.2 已将 DAG GPU 分配入口委托给 `platform_core.services.gpu_allocator.GPUAllocator`，核心 GPU 调度逻辑由不依赖 Airflow 的自动化测试直接覆盖；DAG 文件同时通过 Python 语法编译检查。
